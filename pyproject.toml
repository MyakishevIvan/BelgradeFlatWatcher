[project]
name = "belgrade-flat-watcher"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "selenium>=4.41.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
